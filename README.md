# Сервис анализа совместимости резюме и вакансий

Онлайн-сервис для анализа совместимости резюме с вакансиями.

## Возможности

- Загрузка резюме (PDF, DOCX, TXT)
- Анализ вакансий по ссылке
- Расчет процента совместимости
- Gap-анализ: что не хватает для соответствия вакансии

## Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

## Установка и запуск

### 🚀 Способ 1: Запуск БЕЗ терминала (САМЫЙ ПРОСТОЙ!)

**Для Mac:**
1. Найдите файл `ЗАПУСК.command` в папке проекта
2. Дважды кликните на него
3. Готово! Браузер откроется автоматически

**Для Windows:**
1. Найдите файл `ЗАПУСК.bat` в папке проекта
2. Дважды кликните на него
3. Готово! Браузер откроется автоматически

> ⚠️ При первом запуске может потребоваться установка зависимостей (произойдет автоматически)

### Способ 2: Автоматический запуск через терминал

1. Откройте терминал в папке проекта
2. Сделайте скрипт исполняемым (если нужно):
   ```bash
   chmod +x run.sh
   ```
3. Запустите скрипт:
   ```bash
   ./run.sh
   ```

Скрипт автоматически установит все зависимости и запустит сервис.

### Способ 2: Ручной запуск

1. Откройте терминал в папке проекта

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
   
   Если у вас несколько версий Python, используйте:
   ```bash
   pip3 install -r requirements.txt
   ```
   
   Или если используете виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # На Windows: venv\Scripts\activate
   pip install -r requirements.txt
   ```

3. Запустите сервис:
   ```bash
   uvicorn main:app --reload
   ```
   
   Или:
   ```bash
   python -m uvicorn main:app --reload
   ```

4. Откройте браузер и перейдите по адресу:
   ```
   http://localhost:8000
   ```

## Использование

1. Откройте http://localhost:8000 в браузере
2. Загрузите ваше резюме (PDF, DOCX или TXT)
3. Вставьте ссылку на вакансию (например, с hh.ru, habr.com и т.д.)
4. Нажмите "Анализировать"
5. Получите анализ совместимости и рекомендации

## Остановка сервиса

Нажмите `Ctrl + C` в терминале, где запущен сервис.

## API Endpoints

- `GET /` - Главная страница
- `POST /api/analyze` - Анализ резюме и вакансии
- `GET /api/health` - Проверка работоспособности

## Устранение проблем

### Ошибка "uvicorn: command not found"
Убедитесь, что зависимости установлены:
```bash
pip install -r requirements.txt
```

### Ошибка "Port 8000 is already in use"
Используйте другой порт:
```bash
uvicorn main:app --reload --port 8001
```

### Проблемы с установкой зависимостей
Попробуйте обновить pip:
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

