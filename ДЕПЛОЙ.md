# 🚀 Деплой на Streamlit Cloud

## Пошаговая инструкция:

### Шаг 1: Создайте GitHub репозиторий

1. Перейдите на https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку "+" в правом верхнем углу
4. Выберите "New repository"
5. Назовите репозиторий (например: `cv-analysis-service`)
6. Выберите "Public" (Streamlit Cloud требует публичный репозиторий)
7. НЕ отмечайте "Initialize with README" (у нас уже есть файлы)
8. Нажмите "Create repository"

### Шаг 2: Подготовьте код локально

Выполните в терминале в папке проекта:

```bash
cd /Users/bettybotter/Desktop/CV-master

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: CV Analysis Service"

# Переименуйте ветку в main (если нужно)
git branch -M main
```

### Шаг 3: Подключите к GitHub

После создания репозитория на GitHub, вы увидите инструкции. Выполните:

```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

Например, если ваш username `bettybotter` и репозиторий `cv-analysis`:
```bash
git remote add origin https://github.com/bettybotter/cv-analysis.git
git push -u origin main
```

### Шаг 4: Деплой на Streamlit Cloud

1. Перейдите на https://share.streamlit.io
2. Войдите через GitHub
3. Нажмите "New app"
4. Выберите ваш репозиторий
5. Выберите ветку `main`
6. Укажите путь к файлу: `streamlit_app.py`
7. Нажмите "Deploy"

### Шаг 5: Готово!

Через несколько минут ваше приложение будет доступно по адресу:
`https://YOUR_APP_NAME.streamlit.app`

---

## Важно для Streamlit Cloud:

✅ Файл `streamlit_app.py` должен быть в корне репозитория  
✅ Файл `requirements.txt` должен быть в корне  
✅ Все зависимости должны быть указаны в `requirements.txt`  
✅ Репозиторий должен быть публичным  

---

## Если возникли проблемы:

1. **Ошибка при push**: Убедитесь, что вы авторизованы в GitHub через git
2. **Ошибка деплоя**: Проверьте, что все зависимости в requirements.txt
3. **Приложение не запускается**: Проверьте логи в Streamlit Cloud

